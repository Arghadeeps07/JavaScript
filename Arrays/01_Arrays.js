// arr.push(), arr.length()
const arr = new Array(1,2,3,4,5);
console.log(arr);

// unshift() adds the element in the front
// shift() delets the first element from the array
// includes()  retuens a bool which shows the element is present or not in the array
// indexof() 
// join() converts the array into a string

// slice(), splice()
// splice() manipulates the original array by removing the range of elements from the original array
// concate() returns a new contatinated array